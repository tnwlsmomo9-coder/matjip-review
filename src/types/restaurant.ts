export interface Restaurant {
  id: string;
  name: string;
  category: string;
  imageUrl: string;
  oneLineReview: string;
  landmarkText: string;
  reviewCount: number;
  trustScore: number;
  saveCount: number;
}
